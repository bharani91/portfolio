<h1>Listing pages</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Content</th>
    <th></th>
  </tr>

<% @pages.each do |page| %>
  <tr>
    <td><%= page.title %></td>
    <td><%= markdown(page.content) %></td>
    <td><%= link_to 'Show', page %></td>
  </tr>
<% end %>
</table>

